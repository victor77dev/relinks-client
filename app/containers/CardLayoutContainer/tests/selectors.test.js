// import { fromJS } from 'immutable';
// import { selectCardLayoutContainerDomain } from '../selectors';

describe('selectCardLayoutContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
